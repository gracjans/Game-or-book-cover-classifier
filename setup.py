#!/usr/bin/env python3.9

from setuptools import find_packages, setup

if __name__ == "__main__":
    setup(
        name="game_or_book_cover_classifier",
        version="0.0.0",
        author="Gracjan Strzelec",
        packages=find_packages(),
    )